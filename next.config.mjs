/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
  domains: ['www.bhphotovideo.com', 'rentout-pro.s3.amazonaws.com','static.bhphoto.com'],
  },
  };
  export default nextConfig;
